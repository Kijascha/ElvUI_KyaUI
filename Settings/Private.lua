local KYA, E, L, V, P, G = unpack(select(2, ...))

-- Setup Private Table
V.kyaui = {
}

V.kyaui.minimapButtons = {
    enabled = true,
    bars = {
        buttonGrid = {
            slots = {},
        },
        quickAccessBar = {
            slots = {},
        },
    },
}